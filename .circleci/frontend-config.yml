# TODO tests are not yet completed
version: 2.1

jobs:
  e2e-tests:
    docker:
      - image: node:latest

    steps:
      - checkout

      - run:
          name: FRONTEND
          command: echo FRONTEND

workflows:
  the-great-gatsby-test:
    jobs:
      - e2e-tests:
          filters:
            branches:
              only:
                - main
